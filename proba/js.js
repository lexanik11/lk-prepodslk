$(document).ready(function () {

	$('.one').on('click', function () {
		$('.one-list').toggleClass('selected');
	});

	$('.two').on('click', function () {
		$('.two-list').toggleClass('selected');
	});

	$('.three').on('click', function () {
		$('.three-list').toggleClass('selected');
	});

});